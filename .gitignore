node_modules/
.env
# Environment-specific secrets (.env.prod etc). .env.example is the only one
# safe to commit. Note .env.test was already tracked before this rule existed,
# so gitignore does not untrack it -- remove it from the index separately.
.env.*
!.env.example
*.db
reports/
backups/
dist/
.vercel/
coverage/
backend/tests/.tmp/
PRODUCT_OVERVIEW.html
PRODUCT_OVERVIEW.md
royalmart-billing-structure.html

# Billing -- carries bank account no., IFSC and PAN. Never commit to the
# client's repo. The agent file and its ledger are local-only.
.claude/agents/billingAgent.md
.claude/billing/

# Agent-facing working docs -- kept local, not committed.
CLAUDE.md
docs/DOMAIN.md
docs/RUNBOOK.md

# Stale READMEs superseded by the agent docs above -- kept local only. These
# were tracked before this rule, so the patterns below do not untrack them on
# their own: that was done separately with `git rm --cached`.
backend/tests/README.md
frontend/README.md
frontend/src/test/README.md
